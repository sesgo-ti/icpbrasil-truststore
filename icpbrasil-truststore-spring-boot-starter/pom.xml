<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>br.gov.go.saude</groupId>
    <artifactId>icpbrasil-truststore</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>icpbrasil-truststore-spring-boot-starter</artifactId>
  <name>ICP-Brasil TrustStore — Spring Boot Starter</name>
  <description>Auto-configuração Spring Boot, controller REST e ponto de entrada standalone para o icpbrasil-truststore.</description>

  <dependencies>
    <!-- Implementação completa -->
    <dependency>
      <groupId>br.gov.go.saude</groupId>
      <artifactId>icpbrasil-truststore-core</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Spring Boot Web — @RestController, @SpringBootApplication -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Gerador de metadados para autocomplete no IDE -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-configuration-processor</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- Lombok -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- Testes -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>repackage</id>
            <goals>
              <goal>repackage</goal>
            </goals>
            <configuration>
              <!-- Desativado por padrão: o módulo é tanto lib quanto executável standalone.
                   Ative com -Pstandalone para gerar o fat-jar. -->
              <skip>true</skip>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>standalone</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>repackage</id>
                <goals><goal>repackage</goal></goals>
                <configuration>
                  <skip>false</skip>
                  <classifier>standalone</classifier>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
